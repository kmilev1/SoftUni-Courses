function histogram(input) {
    let count = (input[0]);
    let sum = "";

    for (let i = 1; i <= 10; i++)  {
        sum = sum + "*";
        console.log(sum)
    }
} 

histogram(["*"])
